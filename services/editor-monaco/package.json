{
  "name": "@edd/editor-monaco",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build && esbuild src/run.ts --bundle --minify --platform=node --target=node22 --format=esm --external:node-pty --log-level=warning '--banner:js=import { createRequire } from \"module\"; const require = createRequire(import.meta.url);' --outfile=dist/server.js",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@edd/config": "workspace:*",
    "@types/node": "^26.3.0",
    "@types/ws": "^8.18.1",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "esbuild": "^0.28.2",
    "fast-check": "^4.9.0",
    "monaco-editor": "^0.56.0",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11",
    "ws": "^8.21.3"
  }
}
