{
  "name": "@edd/editor-monaco",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build && esbuild src/run.ts --bundle --platform=node --target=node22 --format=esm --external:node-pty --log-level=warning '--banner:js=import { createRequire } from \"module\"; const require = createRequire(import.meta.url);' --outfile=dist/server.js",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@edd/config": "workspace:*",
    "@types/node": "^26.1.1",
    "@types/ws": "^8.18.1",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "esbuild": "^0.28.1",
    "fast-check": "^4.9.0",
    "monaco-editor": "^0.55.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10",
    "ws": "^8.21.0"
  }
}
