<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->

# infra/images

Curated **golden base images** for workspaces (OS + toolchain + code-server +
sshd + idle-agent), published to ECR and surfaced in the admin
catalog. Extensions are sourced from **Open VSX** (not the MS marketplace).

Dockerfiles + build/scan CI land in Phase 1.
